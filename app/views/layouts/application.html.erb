<!DOCTYPE html>
<html lang="en" class="dark">
  <head>
    <title>My Tasks</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="bg-gradient-to-br from-black to-gray-900 text-white font-sans min-h-screen relative overflow-x-hidden">

    <!-- Animated Background Glow -->
    <div class="absolute inset-0 z-0 pointer-events-none animate-pulse bg-gradient-to-tr from-pink-500/10 via-blue-500/10 to-purple-500/10 blur-3xl"></div>

    <!-- Navigation/Header -->
    <header class="relative z-10 px-6 py-4 flex justify-between items-center bg-white/5 backdrop-blur-md border-b border-white/10 shadow">
      <h1 class="text-2xl font-extrabold tracking-tight">📝 My Tasks</h1>
      <button id="dark-mode-toggle" class="px-4 py-2 rounded bg-gray-700 text-white hover:bg-gray-600 transition">Toggle Dark Mode</button>
    </header>

    <main>
      <%= yield %>
    </main>
  </body>
</html>
